<UserControl x:Class="Documentor.Presentation.Views.Pages.DocumentPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Documentor.Presentation.Views.Pages"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="{Binding Title}"
                           Style="{StaticResource PageTitleTextStyle}"/>
                <TextBlock Text="{Binding ActiveFilterSummary}"
                           Style="{StaticResource SectionHintTextStyle}"
                           Margin="0,6,0,0"/>
            </StackPanel>

            <TextBlock Grid.Column="1"
                       Text="{Binding TotalCount, StringFormat=Всего: {0}}"
                       Style="{StaticResource SectionHintTextStyle}"
                       VerticalAlignment="Center"/>
        </Grid>

        <!-- Action bar -->
        <Border Grid.Row="2"
                Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Верхняя строка -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Фильтр + сброс -->
                    <Grid Grid.Column="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Command="{Binding OpenFilterCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0"
                                MinWidth="120">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="FilterOutline"
                                                            Width="16"
                                                            Height="16"
                                                            Margin="0,0,8,0"/>
                                <TextBlock Text="Фильтр"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="1"
                                Command="{Binding ClearFilterCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Width="42"
                                MinWidth="42"
                                Padding="0"
                                Margin="1,0,0,0"
                                ToolTip="Сбросить фильтр">
                            <iconPacks:PackIconMaterial Kind="Close"
                                                        Width="16"
                                                        Height="16"/>
                        </Button>
                    </Grid>

                    <!-- Обновить -->
                    <Button Grid.Column="4"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0"
                            ToolTip="Обновить список">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Refresh"
                                                        Width="16"
                                                        Height="16"
                                                        Margin="0,0,8,0"/>
                            <TextBlock Text="Обновить"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- Вторая строка -->
                <WrapPanel Grid.Row="2">

                    <Button Command="{Binding DeleteDocumentCommand}"
                            Background="#C62828"
                            Foreground="White"
                            BorderBrush="#C62828"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="DeleteOutline"
                                                        Width="16"
                                                        Height="16"
                                                        Margin="0,0,8,0"/>
                            <TextBlock Text="Удалить"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </WrapPanel>
            </Grid>
        </Border>

        <!-- Data -->
        <Border Grid.Row="4"
                Style="{StaticResource CardBorderStyle}">
            <Grid>
                <DataGrid x:Name="DocumentsGrid"
                          ItemsSource="{Binding Documents}"
                          SelectedItem="{Binding SelectedDocument}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          Sorting="DocumentsGrid_OnSorting"
                          Style="{StaticResource AppDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Название"
                                            Binding="{Binding Title}"
                                            Width="212"
                                            SortMemberPath="Title"
                                            ElementStyle="{StaticResource AppDataGridTextStyle}"/>

                        <DataGridTextColumn Header="Создан"
                                            Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy HH:mm}"
                                            Width="260"
                                            SortMemberPath="CreatedAt"
                                            ElementStyle="{StaticResource AppDataGridTextStyle}"/>

                        <DataGridTextColumn Header="Тип документа"
                                            Binding="{Binding TypeDisplay}"
                                            Width="140"
                                            SortMemberPath="Type"
                                            ElementStyle="{StaticResource AppDataGridTextStyle}"/>

                        <DataGridTextColumn Header="Шаблон"
                                            Binding="{Binding TemplateTitle}"
                                            Width="220"
                                            SortMemberPath="TemplateId"
                                            ElementStyle="{StaticResource AppDataGridTextStyle}"/>
                        <DataGridTemplateColumn Header=""
                                                Width="54">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.DownloadDocumentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Width="28"
                                            Height="28"
                                            MinWidth="28"
                                            Padding="0"
                                            Margin="0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Background="{DynamicResource Brush.SurfaceAlt}"
                                            BorderBrush="{DynamicResource Brush.Border}"
                                            BorderThickness="1"
                                            Cursor="Hand"
                                            ToolTip="Скачать документ">
                                        <iconPacks:PackIconMaterial Kind="DownloadOutline"
                                                                    Width="15"
                                                                    Height="15"
                                                                    Foreground="{DynamicResource Brush.TextPrimary}"
                                                                    HorizontalAlignment="Center"
                                                                    VerticalAlignment="Center"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                    </DataGrid.Columns>
                </DataGrid>

                <Border Background="{DynamicResource Brush.Surface}"
                        BorderBrush="{DynamicResource Brush.Border}"
                        BorderThickness="1"
                        Padding="20"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <TextBlock Text="Нет созданных документов"
                               FontWeight="Bold"
                               FontSize="14"
                               Foreground="{DynamicResource Brush.TextPrimary}"/>
                </Border>
            </Grid>
        </Border>

        <!-- Paging -->
        <Border Grid.Row="6"
                Style="{StaticResource CardBorderStyle}"
                Padding="12">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <Button Command="{Binding PreviousPageCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Width="40"
                        MinWidth="40"
                        Padding="0"
                        Margin="0,0,12,0"
                        ToolTip="Предыдущая страница">
                    <iconPacks:PackIconMaterial Kind="ChevronLeft"
                                                Width="18"
                                                Height="18"/>
                </Button>

                <TextBlock Text="{Binding CurrentPage, StringFormat=Страница {0}}"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Brush.TextPrimary}"/>

                <TextBlock Text="{Binding TotalPages, StringFormat=из {0}}"
                           VerticalAlignment="Center"
                           Margin="0,0,12,0"
                           Foreground="{DynamicResource Brush.TextSecondary}"/>

                <Button Command="{Binding NextPageCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Width="40"
                        MinWidth="40"
                        Padding="0"
                        Margin="0"
                        ToolTip="Следующая страница">
                    <iconPacks:PackIconMaterial Kind="ChevronRight"
                                                Width="18"
                                                Height="18"/>
                </Button>
            </StackPanel>
        </Border>

        <!-- Error -->
        <TextBlock Grid.Row="7"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource Brush.Error}"
                   Margin="0,12,0,0"
                   TextWrapping="Wrap"/>
    </Grid>
</UserControl>
