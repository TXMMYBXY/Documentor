<UserControl x:Class="Documentor.Presentation.Views.Controls.InAppToastHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:toasts="clr-namespace:Documentor.Presentation.Services.Toasts"
             xmlns:enums="clr-namespace:Documentor.Core.Enums">

    <UserControl.Resources>
        <Style x:Key="ToastCardBaseStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Background" Value="{DynamicResource Brush.SurfaceAlt}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <DataTemplate DataType="{x:Type toasts:ToastItemViewModel}">
            <Border>
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource ToastCardBaseStyle}">
                        <Style.Triggers>
                            <!-- SUCCESS -->
                            <DataTrigger Binding="{Binding Severity}" Value="{x:Static enums:NotificationSeverity.Success}">
                                <Setter Property="Background" Value="{DynamicResource Brush.Toast.SuccessBackground}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Toast.SuccessBorder}"/>
                            </DataTrigger>

                            <!-- ERROR -->
                            <DataTrigger Binding="{Binding Severity}" Value="{x:Static enums:NotificationSeverity.Error}">
                                <Setter Property="Background" Value="{DynamicResource Brush.Toast.ErrorBackground}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Toast.ErrorBorder}"/>
                            </DataTrigger>

                            <!-- fade in -->
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1"
                                                         Duration="0:0:0.16"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <!-- fade out -->
                            <DataTrigger Binding="{Binding IsClosing}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             To="0"
                                                             Duration="0:0:0.25"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- ICON -->
                    <iconPacks:PackIconMaterial Grid.Row="0"
                                                Grid.Column="0"
                                                Width="20"
                                                Height="20"
                                                VerticalAlignment="Top">
                        <iconPacks:PackIconMaterial.Style>
                            <Style TargetType="iconPacks:PackIconMaterial">
                                <Setter Property="Kind" Value="InformationOutline"/>
                                <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}"/>

                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Severity}" Value="{x:Static enums:NotificationSeverity.Success}">
                                        <Setter Property="Kind" Value="CheckCircleOutline"/>
                                        <Setter Property="Foreground" Value="{DynamicResource Brush.Success}"/>
                                    </DataTrigger>

                                    <DataTrigger Binding="{Binding Severity}" Value="{x:Static enums:NotificationSeverity.Error}">
                                        <Setter Property="Kind" Value="CloseCircleOutline"/>
                                        <Setter Property="Foreground" Value="{DynamicResource Brush.Error}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </iconPacks:PackIconMaterial.Style>
                    </iconPacks:PackIconMaterial>

                    <!-- TEXT -->
                    <StackPanel Grid.Row="0"
                                Grid.Column="2">
                        <TextBlock Text="{Binding Title}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource Brush.TextPrimary}"
                                   TextWrapping="Wrap"
                                   MaxWidth="360"/>

                        <TextBlock Text="{Binding Message}"
                                   Margin="0,4,0,0"
                                   Foreground="{DynamicResource Brush.TextSecondary}"
                                   TextWrapping="Wrap"
                                   MaxWidth="360"/>
                    </StackPanel>

                    <!-- CLOSE -->
                    <Button Grid.Row="0"
                            Grid.Column="3"
                            Command="{Binding CloseCommand}"
                            Width="24"
                            Height="24"
                            Padding="0"
                            Margin="0,-2,0,0"
                            Background="Transparent"
                            BorderThickness="0"
                            ToolTip="Закрыть">
                        <iconPacks:PackIconMaterial Kind="Close"
                                                    Width="14"
                                                    Height="14"
                                                    Foreground="{DynamicResource Brush.TextSecondary}"/>
                    </Button>

                    <!-- ACTION BUTTON -->
                    <StackPanel Grid.Row="1"
                                Grid.Column="2"
                                Grid.ColumnSpan="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,10,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasAction}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <Button Command="{Binding ActionCommand}"
        Width="34"
        Height="34"
        MinWidth="34"
        Padding="0"
        ToolTip="{Binding ActionText}">
        <Button.Style>
            <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                <Setter Property="Content">
                    <Setter.Value>
                        <Grid Width="16" Height="16">
                            <iconPacks:PackIconMaterial Kind="DownloadOutline"
                                                        Width="16"
                                                        Height="16"
                                                        Foreground="{DynamicResource Brush.TextPrimary}">
                                <iconPacks:PackIconMaterial.Style>
                                    <Style TargetType="iconPacks:PackIconMaterial">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActionBusy}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </iconPacks:PackIconMaterial.Style>
                            </iconPacks:PackIconMaterial>

                            <ProgressBar IsIndeterminate="True"
                                         Height="6"
                                         VerticalAlignment="Center"
                                         Visibility="Collapsed">
                                <ProgressBar.Style>
                                    <Style TargetType="ProgressBar">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActionBusy}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ProgressBar.Style>
                            </ProgressBar>
                        </Grid>
                    </Setter.Value>
                </Setter>

                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsActionBusy}" Value="True">
                        <Setter Property="IsEnabled" Value="False"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Button.Style>
    </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <!-- Должно быть True, иначе кнопка не нажимается -->
    <Grid IsHitTestVisible="True">
        <ItemsControl ItemsSource="{Binding Notifications}"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Bottom">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel VerticalAlignment="Bottom"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Grid>
</UserControl>