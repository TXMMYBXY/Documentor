<mah:MetroWindow x:Class="Documentor.Presentation.Views.Dialogs.Statement.AddTemplateDialogWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 Title="Добавление шаблона заявления"
                 Width="580"
                 SizeToContent="Height"
                 MinHeight="320"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterOwner"
                 TitleCharacterCasing="Normal"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}">
    <Grid Margin="20">
        <Border Style="{StaticResource CardBorderStyle}">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Добавление шаблона заявления"
                           Style="{StaticResource PageTitleTextStyle}"/>

                <StackPanel Grid.Row="2">
                    <TextBlock Text="Название"
                               Style="{StaticResource FormLabelTextStyle}"/>

                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource InputTextBoxStyle}"
                             Margin="0,0,0,12"/>

                    <TextBlock Text="Файл шаблона"
                               Style="{StaticResource FormLabelTextStyle}"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding FilePath}"
                                 IsReadOnly="True"
                                 Style="{StaticResource InputTextBoxStyle}"/>

                        <Button Grid.Column="1"
                                Content="Обзор..."
                                Command="{Binding BrowseFileCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="10,0,0,0"/>
                    </Grid>

                    <CheckBox Content="Активен"
                              IsChecked="{Binding IsActive, Mode=TwoWay}"
                              Margin="0,0,0,12"/>

                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="{DynamicResource Brush.Error}"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Row="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="Отмена"
                            Command="{Binding CancelCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"/>

                    <Button Content="Сохранить"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</mah:MetroWindow>