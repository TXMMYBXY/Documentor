<mah:MetroWindow x:Class="Documentor.Presentation.Views.Windows.MainShellWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 Title="Documentor"
                 Width="1280"
                 Height="800"
                 TitleCharacterCasing="Normal"
                 WindowStartupLocation="CenterScreen"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}">
    <Grid Background="{DynamicResource Brush.WindowBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding IsNavigationPaneExpanded, Converter={StaticResource BoolToGridLengthConverter}}"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{DynamicResource Brush.SidebarBackground}"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="0,0,1,0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="14"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="18"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Toggle -->
                <Button Grid.Row="0"
                        Command="{Binding ToggleNavigationPaneCommand}"
                        Width="44"
                        Height="44"
                        Padding="0"
                        HorizontalAlignment="{Binding IsNavigationPaneExpanded, Converter={StaticResource SidebarToggleAlignmentConverter}}">
                    <iconPacks:PackIconMaterial Kind="Menu"
                                                Width="20"
                                                Height="20"/>
                </Button>

                <!-- Title -->
                <TextBlock Grid.Row="2"
                           Text="{Binding Title}"
                           Foreground="{DynamicResource Brush.SidebarText}"
                           FontSize="24"
                           FontWeight="Bold"
                           TextWrapping="Wrap"
                           Visibility="{Binding IsNavigationPaneExpanded, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <!-- Expanded menu -->
                <ItemsControl Grid.Row="4"
                              ItemsSource="{Binding MenuItems}"
                              Visibility="{Binding IsNavigationPaneExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding Command}"
                                    ToolTip="{Binding Title}">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SidebarMenuButtonStyle}">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Padding" Value="12,0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource Brush.SidebarItemSelected}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource Brush.TextOnAccent}"/>
                                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.SidebarItemSelected}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="24"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <iconPacks:PackIconMaterial Grid.Column="0"
                                                                Kind="{Binding IconKind}"
                                                                Width="20"
                                                                Height="20"
                                                                VerticalAlignment="Center"
                                                                HorizontalAlignment="Center"/>

                                    <TextBlock Grid.Column="2"
                                               Text="{Binding Title}"
                                               VerticalAlignment="Center"
                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Collapsed menu -->
                <ItemsControl Grid.Row="4"
                              ItemsSource="{Binding MenuItems}"
                              HorizontalAlignment="Center"
                              Visibility="{Binding IsNavigationPaneExpanded, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding Command}"
                                    Width="44"
                                    Height="44"
                                    Padding="0"
                                    HorizontalAlignment="Center"
                                    ToolTip="{Binding Title}">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SidebarMenuButtonStyle}">
                                        <Setter Property="Margin" Value="0,0,0,6"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource Brush.SidebarItemSelected}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource Brush.TextOnAccent}"/>
                                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.SidebarItemSelected}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>

                                <iconPacks:PackIconMaterial Kind="{Binding IconKind}"
                                                            Width="20"
                                                            Height="20"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Expanded logout -->
                <Button Grid.Row="5"
                        Command="{Binding LogoutCommand}"
                        Margin="0"
                        ToolTip="Выйти"
                        Visibility="{Binding IsNavigationPaneExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="12,0"/>
                        </Style>
                    </Button.Style>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <iconPacks:PackIconMaterial Grid.Column="0"
                                                    Kind="Logout"
                                                    Width="20"
                                                    Height="20"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Center"/>

                        <TextBlock Grid.Column="2"
                                   Text="Выйти"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <!-- Collapsed logout -->
                <Button Grid.Row="5"
                        Command="{Binding LogoutCommand}"
                        Width="44"
                        Height="44"
                        Padding="0"
                        HorizontalAlignment="Center"
                        ToolTip="Выйти"
                        Visibility="{Binding IsNavigationPaneExpanded, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </Button.Style>

                    <iconPacks:PackIconMaterial Kind="Logout"
                                                Width="20"
                                                Height="20"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Column="1"
              Background="{DynamicResource Brush.WindowBackground}">
            <ContentControl Content="{Binding CurrentPage}"
                            Margin="20"/>
        </Grid>
    </Grid>
</mah:MetroWindow>